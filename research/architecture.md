# Architecture

Runtime concept:

`event -> context retrieval -> active drives -> first intuition -> analysis gate -> decision arbitration -> expression -> outcome attribution -> memory/state/growth update -> continuity check`

Change hierarchy:

`temporary_state -> process_memory -> conditional_policy -> domain_strategy -> stable_trait`

Identity continuity should avoid both static freeze and unconstrained drift. This is a project hypothesis, not an established scientific formula.
